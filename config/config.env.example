# config.env.example - 환경 설정 예시
# smol.ai 한국어 뉴스 자동 발행 시스템
#
# 이 파일을 config.env로 복사하고 실제 값을 입력하세요.

# Web 레포지토리 경로
WEB_REPO_PATH=/home/jonhpark/workspace/web

# CLI 경로 (기본값 사용 시 주석 처리)
# CODEX_BIN=/home/jonhpark/.npm-global/bin/codex
# CLAUDE_BIN=/home/jonhpark/.local/bin/claude

# 로그 레벨 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# 모델 설정 (기본값 사용 시 주석 처리)
# CODEX_MODEL=gpt-5.2
# CODEX_REASONING_EFFORT=high
# CLAUDE_MODEL=opus

# 재시도 설정
# MAX_RETRIES=3
# RETRY_DELAY=5
# MAX_REVIEW_RETRIES=1

# 알림 설정 (Slack Webhook URL - 선택사항)
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz
